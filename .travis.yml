language: java
sudo: false
jdk:
- oraclejdk8
script:
- mvn package
- (cd test-integration && ./test-openwayback.sh)
- (cd test-integration && ./test-pywb.sh)
after_success:
- mvn jacoco:report coveralls:report
addons:
  apt:
    packages:
    - libsnappy-dev
    - python-virtualenv
